# Dependency directories
node_modules/
jspm_packages/
typings/

# Build output
dist/
build/
out/
tsbuildinfo

# Environment variables (CRITICAL: Never commit these)
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
.env.sample        # Don't ignore the sample/template, only the actual secrets
!.env.example      # Keep the example file in git

# Prisma
# We keep migrations, but ignore the SQLite local DB if you ever use it for testing
*.db
*.db-journal

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pino-pretty.log    # If you ever pipe pino output to a file

# OS metadata
.DS_Store
Thumbs.db

# IDEs/Editors
.vscode/
.idea/
*.swp
*.swo

# Diagnostic tools
.npm
.eslintcache

# Special scrap files
planning/